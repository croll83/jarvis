# Copia questo file in `keystore.properties` (gitignored) e compila i valori reali.
# NON committare mai keystore.properties né il file .keystore.
#
# Crea il keystore una volta sola (vedi DISTRIBUTION.md) e riusalo per SEMPRE:
# se lo perdi non puoi più aggiornare l'app installata con la stessa firma.

storeFile=jarvis-release.keystore
storePassword=CAMBIA_ME
keyAlias=jarvis
keyPassword=CAMBIA_ME
