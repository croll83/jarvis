package com.jarvis.voice.mobile.ui

import androidx.compose.ui.graphics.Color

/** Palette derivata dalla dashboard HA custom "Wagmi": navy→viola, cyan primario, oro/verde. */
object JarvisColors {
    val bgTop = Color(0xFF090A12)      // nero-navy in alto
    val bgMid = Color(0xFF12102A)      // viola scuro
    val glowPurple = Color(0xFF43206A) // glow magenta/viola in basso
    val glowCyan = Color(0xFF1E6FA0)

    val cyan = Color(0xFF3FC6F0)
    val cyanSoft = Color(0xFF67D6FA)
    val purple = Color(0xFF9A6BE0)
    val gold = Color(0xFFE5A83B)
    val green = Color(0xFF67E0A0)
    val red = Color(0xFFFF5A6A)

    val textPrimary = Color(0xFFF2F4F8)
    val textMuted = Color(0xFF8A8FA3)

    val panel = Color(0x14FFFFFF)        // white 8% — pannello glassy
    val panelBorder = Color(0x26FFFFFF)  // white 15%
}
