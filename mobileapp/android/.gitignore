*.iml
.gradle/
/local.properties
/.idea/
.DS_Store
/build
**/build/
/captures
.externalNativeBuild
.cxx
local.properties
# Wrapper jar/scripts are generated by Android Studio or `gradle wrapper`
!gradle/wrapper/gradle-wrapper.properties
# Secrets: URL/token orchestrator non vanno committati
secrets.properties
# Asset Rive di marketplace: non ridistribuire via git (copiare a mano su ogni macchina).
# Se il repo è privato e la licenza lo consente, rimuovi questa riga per committarlo.
mobile/src/main/assets/*.riv
