{
  "id": "browser-dom",
  "name": "Browser DOM",
  "description": "Direct DOM manipulation tools for browser automation — CSS/XPath selectors, intelligent waits, and JavaScript evaluation via CDP, without snapshot refs.",
  "configSchema": {
    "type": "object",
    "additionalProperties": false,
    "properties": {
      "cdpUrl": {
        "type": "string",
        "description": "Chrome DevTools Protocol endpoint URL (e.g. http://127.0.0.1:18800)"
      },
      "defaultTimeoutMs": {
        "type": "number",
        "description": "Default timeout in ms for DOM operations (default: 15000)"
      }
    }
  },
  "uiHints": {
    "cdpUrl": {
      "label": "CDP URL",
      "placeholder": "http://127.0.0.1:18800"
    },
    "defaultTimeoutMs": {
      "label": "Default Timeout (ms)",
      "placeholder": "15000"
    }
  }
}
